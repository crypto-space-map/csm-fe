import { SocialPanel } from './ social-panel';
import { StyledFooter } from './styled';

export const Footer = () => (
  <StyledFooter>
    <span />
    <span />

    <SocialPanel />
  </StyledFooter>
);
