import { Loader as CommonLoader } from 'common/components/loader';

import { LoaderWrapper } from '../../styles';

export const Loader = () => (
  <LoaderWrapper>
    <CommonLoader />
  </LoaderWrapper>
);
