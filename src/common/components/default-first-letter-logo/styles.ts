import styled from '@emotion/styled';
import { gradientBackground } from 'global/styles';

export const LetterLogoWrapper = styled.div`
  width: inherit;
  height: inherit;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 20px;
  ${gradientBackground}
`;
